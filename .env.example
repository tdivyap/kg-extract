# Copy to .env, then paste your key:
#   cp .env.example .env
# .env is gitignored — never commit it.

OPENAI_API_KEY=sk-proj-...

# Optional, only for non-standard OpenAI endpoints (Azure, proxy, etc.):
# OPENAI_BASE_URL=https://api.openai.com/v1
